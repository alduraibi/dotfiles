# Environmental Variables

## XDG
### XDG system directories
XDG_DATA_DIRS       DEFAULT=/usr/local/share:/usr/share
XDG_CONFIG_DIRS     DEFAULT=/etc/xdg
### XDG hidden user directories
XDG_CONFIG_HOME     DEFAULT=@{HOME}/.config
XDG_CACHE_HOME      DEFAULT=@{HOME}/.cache
XDG_DATA_HOME       DEFAULT=@{HOME}/.local/share
#XDG_RUNTIME_DIR    DEFAULT=@{HOME}/
### XDG user directories
XDG_DESKTOP_DIR     DEFAULT=@{HOME}/Desktop
XDG_DOCUMENTS_DIR   DEFAULT=@{HOME}/Documents
XDG_DOWNLOAD_DIR    DEFAULT=@{HOME}/Downloads
XDG_MUSIC_DIR       DEFAULT=@{HOME}/Music
XDG_PICTURES_DIR    DEFAULT=@{HOME}/Pictures
XDG_PUBLICSHARE_DIR DEFAULT=@{HOME}/Public
XDG_TEMPLATES_DIR   DEFAULT=@{HOME}/Templates
XDG_VIDEOS_DIR      DEFAULT=@{HOME}/Videos

## Defaults

### Default Programs
# Text editor
EDITOR              DEFAULT=nvim
VISUAL              DEFAULT=nvim
# Internet Browser
BROWSER             DEFAULT=firefox
# Terminal emulator
TERMINAL            DEFAULT=kitty

### Default Custom
## Font Style
#FONT                DEFAULT=FiraGO
## Font Size
#FONT_SIZE                DEFAULT=
## Monospaced Font Style
#FONT_MONO           DEFAULT=Fira Code
## Monospaced Font Size
#FONT_MONO_SIZE           DEFAULT=

### Default Misc
# Number of lines saved
#HISTFILESIZE        DEFAULT=10000
#HISTSIZE            DEFAULT=10000
# Path to shell history file
#HISTFILE            DEFAULT=${XDG_DATA_HOME}/zsh/history

## Programs

### bemenu
# Run bemenu with Wayland
BEMENU_BACKEND     DEFAULT=wayland
BEMENU_OPTS        DEFAULT="-i"

### Clutter
# Run Clutter apps on Wayland
CLUTTER_BACKEND     DEFAULT=wayland

### ELinks
# Path to configs
#ELINKS_CONFDIR      DEFAULT=${XDG_DATA_HOME}/elinks

### Firefox
# Enable Wayland
MOZ_ENABLE_WAYLAND  DEFAULT=1
#MOZ_DBUS_REMOTE = 1

### Go
# Path to configs
GOPATH              DEFAULT=${XDG_DATA_HOME}/go

### GTK 1 & 2
# Path to configs
GTK_RC_FILES        DEFAULT=${XDG_CONFIG_HOME}/gtk-1.0/gtkrc
GTK2_RC_FILES       DEFAULT=${XDG_CONFIG_HOME}/gtk-2.0/gtkrc

### QT 5
# Run QT 5 apps with Wayland plugin
QT_QPA_PLATFORM     DEFAULT=wayland
#QT_WAYLAND_DISABLE_WINDOWDECORATION=1
QT_QPA_PLATFORMTHEME    DEFAULT=qt5ct

### RUST CARGO
# Path to configs
CARGO_HOME          DEFAULT=${XDG_DATA_HOME}/cargo

### RUST RUSTUP
# Path to configs
RUSTUP_HOME         DEFAULT=${XDG_DATA_HOME}/rustup

### SDL2
# Run SDL2 apps on Wayland
SDL_VIDEODRIVER     DEFAULT=wayland

### SQLite
# Path to history file
SQLITE_HISTORY      DEFAULT=${XDG_DATA_HOME}/sqlite_history

### zsh
# Path to configs
ZDOTDIR             DEFAULT=${XDG_CONFIG_HOME}/zsh
