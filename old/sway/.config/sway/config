#
### Default config for sway
#

# Read `man 5 sway` for a complete reference.

# Indicate that a program is run using XWayland
for_window [shell="xwayland"] title_format "%title [XWayland]"

include var.d/*
include config.d/*
include autostart
